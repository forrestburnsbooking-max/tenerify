Drop Suno-exported mp3s here — the radio page lists every `.mp3` in this folder automatically.

Naming becomes the displayed title, e.g. `sunset-over-teide.mp3` → "sunset over teide". No manifest to edit.
